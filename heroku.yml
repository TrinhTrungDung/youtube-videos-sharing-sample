setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: youtube_sharing_db
build:
  docker:
    backend: backend/Dockerfile
    frontend: frontend/Dockerfile
run:
  web:
    command:
      - npm start
    image: frontend
